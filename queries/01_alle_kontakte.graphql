# Stufe 2: json-graphql-server (Port 3000)
# Stufe 3: FastAPI + Strawberry (Port 8000/graphql)
# Zeigt: Alle Kontakte – einfachste Abfrage, implizites Schema genügt
query AlleKontakte {
  allContacts {
    name
    species
  }
}
